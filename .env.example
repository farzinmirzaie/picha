# Supabase (weight ledger) — copy to .env for local builds with live data.
# Without these the build quietly falls back to the seed ledger in picha.ts.
# Values: Supabase dashboard → Project Settings → Data API.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<anon-public-key>
